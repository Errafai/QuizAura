<style>
  .contact-form {
  background: rgba(30, 41, 59, 0.3);
  backdrop-filter: blur(12px);
  border-radius: 1.5rem;
  padding: 3rem;
  max-width: 800px;
  margin: 3rem auto;
  border: 1px solid rgba(148, 163, 184, 0.1);
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
}

.contact-form h2 {
  font-size: 2.5rem;
  font-weight: 700;
  margin-bottom: 1.5rem;
  background: linear-gradient(to right, #f8fafc, #3b82f6);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}

.contact-form p {
  color: #94a3b8;
  font-size: 1.1rem;
  line-height: 1.6;
}

.contact-form p a {
  color: #3b82f6;
  text-decoration: none;
  position: relative;
  transition: color 0.3s ease;
}

.contact-form p a:hover {
  color: #60a5fa;
}

.contact-form p a::after {
  content: '';
  position: absolute;
  width: 100%;
  height: 2px;
  bottom: -2px;
  left: 0;
  background: #3b82f6;
  transform: scaleX(0);
  transition: transform 0.3s ease;
}

.contact-form p a:hover::after {
  transform: scaleX(1);
}

.contact-form .form-label {
  color: #f8fafc;
  font-weight: 500;
  margin-bottom: 0.5rem;
}

.contact-form .form-control {
  background: rgba(15, 23, 42, 0.6);
  border: 1px solid rgba(148, 163, 184, 0.2);
  border-radius: 0.75rem;
  padding: 0.75rem 1rem;
  color: #f8fafc;
  transition: all 0.3s ease;
}

.contact-form .form-control:focus {
  background: rgba(15, 23, 42, 0.8);
  border-color: #3b82f6;
  box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2);
  outline: none;
}

.contact-form .form-control::placeholder {
  color: #64748b;
}

.contact-form textarea.form-control {
  resize: vertical;
  min-height: 120px;
}

.btn-submit {
  background: #3b82f6;
  border: none;
  padding: 0.875rem 2rem;
  font-weight: 600;
  border-radius: 0.75rem;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  position: relative;
  overflow: hidden;
}

.btn-submit:hover {
  background: #2563eb;
  transform: translateY(-2px);
  box-shadow: 0 8px 20px -4px rgba(59, 130, 246, 0.5);
}

.btn-submit::after {
  content: '';
  position: absolute;
  width: 100%;
  height: 100%;
  background: linear-gradient(45deg, transparent 25%, rgba(255,255,255,0.1) 50%, transparent 75%);
  top: 0;
  left: -100%;
  transition: 0.5s;
}

.btn-submit:hover::after {
  left: 100%;
}

@media (max-width: 768px) {
  .contact-form {
    padding: 2rem;
    margin: 2rem 1rem;
  }

  .contact-form h2 {
    font-size: 2rem;
  }
}
</style>

<div class="container">
  <div class="contact-form">
    <h2 class="mb-4 text-center">Contact Us</h2>
    <p class="mb-4 text-center">Have any questions or comments? Fill out the form below or email us at <a href="mailto:contact@quizaura.com">contact@quizaura.com</a>.</p>
    <form action="/contact" method="POST">
      <div class="row mb-3">
        <div class="col">
          <label for="firstName" class="form-label">First Name *</label>
          <input type="text" id="firstName" name="firstName" class="form-control" required>
        </div>
        <div class="col">
          <label for="lastName" class="form-label">Last Name *</label>
          <input type="text" id="lastName" name="lastName" class="form-control" required>
        </div>
      </div>
      <div class="mb-3">
        <label for="email" class="form-label">Email *</label>
        <input type="email" id="email" name="email" class="form-control" required>
      </div>
      <div class="mb-3">
        <label for="message" class="form-label">Message *</label>
        <textarea id="message" name="message" class="form-control" rows="5" required></textarea>
      </div>
      <div class="text-center">
        <button type="submit" class="btn btn-submit text-white px-4">Submit</button>
      </div>
    </form>
  </div>